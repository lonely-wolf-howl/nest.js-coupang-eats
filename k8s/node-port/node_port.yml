apiVersion: v1
kind: Service
metadata:
  name: nginx-service

spec:
  selector:
    app: nginx-app
  type: NodePort
  ports:
    - port: 80 # Service Port
      targetPort: 80 # Container Port
      nodePort: 30000 # External Port