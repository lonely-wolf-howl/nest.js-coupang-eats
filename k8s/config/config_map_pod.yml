apiVersion: v1
kind: Pod
metadata:
  name: nginx-app
  labels:
    app: nginx-app

spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
      envFrom:
        - configMapRef:
            name: nginx-config